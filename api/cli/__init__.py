"""CLI opérateur wrench-board — sous-commandes de gestion du pack partagé (T8).

Utilisation engine-side (SSH sur le serveur) :

    python -m api.cli.pack_admin <commande> [options]

V1 Option C : invalidation + inspection uniquement.
Pas de staging tenant-local en V1 → pas de 'promote' (stub V2, rc=3).
"""
